insmod all_video
insmod gfxterm
insmod gfxmenu

set default="ward"
set timeout=0

menuentry "ward" {
    echo "GRUB: Booting ward..."
    probe --set boot_uuid --fs-uuid $root
    multiboot2 /ward boot_uuid=$boot_uuid
	boot
}
